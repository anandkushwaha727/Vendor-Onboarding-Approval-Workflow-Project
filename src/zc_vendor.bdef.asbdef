projection;
strict ( 2 );

use draft;
define behavior for ZC_VENDOR
use etag
{
  use create;
  use update;
  use delete;

  use action Submit;
  use action ApproveByManager;
  use action ApproveByFinance;
  use action Reject;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}